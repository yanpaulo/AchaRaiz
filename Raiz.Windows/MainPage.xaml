<Page
    x:Class="Raiz.WindowsApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Raiz.WindowsApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto">
            </RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" Style="{ThemeResource HeaderTextBlockStyle}">Acha Raízes</TextBlock>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="ExpressionTextBox" Width="300" Padding="5"></TextBox>
                <Button x:Name="OkButton" Width="80" Padding="5" Click="OkButton_Click">Acha</Button>
            </StackPanel>
            <TextBlock x:Name="OutputTextBlock" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Text="Resultado"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
